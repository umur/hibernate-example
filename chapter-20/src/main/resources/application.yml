spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cinetrack_ch20
    username: cinetrack
    password: cinetrack
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        multiTenancy: SCHEMA
  flyway:
    enabled: false  # Manual migration per tenant schema
  security:
    user:
      name: disabled
      password: disabled
